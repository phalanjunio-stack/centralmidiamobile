// Aura Design System — Entry point.
// import { colors, glow, aura, ... } from 'src/theme';
// ou import { theme } from 'src/theme';

import { colors } from './colors';
import { glow } from './glow';
import { motion } from './motion';
import { spacing } from './spacing';
import { radii } from './radii';
import { typography } from './typography';
import { aura } from './aura';
import { smoke } from './smoke';

export { colors, glow, motion, spacing, radii, typography, aura, smoke };

export const theme = { colors, glow, motion, spacing, radii, typography, aura, smoke };
